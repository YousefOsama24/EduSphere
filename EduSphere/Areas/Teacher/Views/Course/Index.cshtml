@using EduSphere.ViewModel;
@model CoursesVM

<div class="container">
    <div class="row mb-3 align-items-Course my-3">
        <div class="col-md-6">
            <h2 class="display-6">All Courses</h2>
        </div>
        @*if (User.IsInRole(SD.SUPER_ADMIN_ROLE) || User.IsInRole(SD.ADMIN_ROLE))
        {*@
            <div class="col-md-6 text-md-end text-start mt-2 mt-md-0">
            <a href="/Teacher/Course/Create" class="btn btn-success"><i class="bi bi-plus-circle"></i> Create</a>
            </div>
        @* } *@
    </div>

    <!-- Filter Section -->
    <section class="py-3 filter-bar">
        <div class="container">
            <div class="row" style="background-color: rgba(var(--bs-light-rgb), var(--bs-bg-opacity)) !important;">
                <div class="col-12">
                    <form>
                        <div class="row">
                            <!-- Course Name -->
                            <div class="col-md-3">
                                <input type="text" class="form-control" placeholder="Name" name="query" value="@ViewBag.Query">
                            </div>

                            <!-- Submit Button -->
                            <div class="col-md-2">
                                <button type="submit" class="btn btn-dark w-100">Filter</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <div class="row">
        <div class="col-12">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Title</th>
                        <th scope="col">CenterId </th>
                        <th scope="col">TeacherId</th>
                        <th scope="col">Description</th>
                        <th scope="col">Price</th>
                        <th scope="col">ThumbnailUrl</th>
                        <th scope="col">CreatedAt</th>
                        <th scope="col">UpdatedAt</th>
                        <th scope="col">IsPublished</th>
                       
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Courses)
                    {
                        <tr>
                            <th scope="row">@item.CourseId</th>
                            <td>@item.Title</td>
                            <td>@item.CenterId </td>
                            <td>@item.TeacherId</td>
                            <td>@item.Description</td>
                            <td>@item.Price</td>
                            <td>@item.ThumbnailUrl</td>
                            <td>@item.CreatedAt</td>
                            <td>@item.UpdatedAt</td>
                            <td>@item.IsPublished</td>
                         
                            <td>
                                <a href="/Teacher/Course/Update/@item.CourseId" type="button" class="btn btn-warning"><i class="bi bi-pencil-square"></i> Edit</a>
                                <form asp-action="Delete"
                                      asp-route-id="@item.CourseId"
                                      method="post"
                                      style="display:inline">

                                    <button type="submit" class="btn btn-danger"
                                            onclick="return confirm('Are you sure you want to delete this choice?')">
                                        <i class="bi bi-trash"></i> Delete
                                    </button>
                                </form>
                               @* @if (User.IsInRole(SD.SUPER_ADMIN_ROLE) || User.IsInRole(SD.ADMIN_ROLE))
                                {
                                   
                                }
                                else
                                {
                                    <span>NoAction</span>
                                }*@
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div class="row">
        <div class="col-12 d-flex justify-content-Course">
            <nav aria-label="Page navigation example">
                <ul class="pagination justify-content-Course">
                    @if (Model.CurrentPage > 1)
                    {
                        <li class="page-item"><a class="page-link" href="/Teacher/Course/Index?page=@(Model.CurrentPage - 1)">Previous</a></li>
                    }
                    @for (var i = 1; i <= Model.TotalPages; i++)
                    {
                        @if (i == Model.CurrentPage)
                        {
                            <li class="page-item active"><a class="page-link" href="/Teacher/Course/Index?page=@i">@i</a></li>
                        }
                        else
                        {
                            <li class="page-item"><a class="page-link" href="/Teacher/Course/Index?page=@i">@i</a></li>
                        }
                    }
                    @if (Model.CurrentPage != Model.TotalPages)
                    {
                        <li class="page-item"><a class="page-link" href="/Teacher/Course/Index?page=@(Model.CurrentPage + 1)">Next</a></li>
                    }
                </ul>
            </nav>
        </div>
    </div>
</div>


<script>
    document.addEventListener("DOMContentLoaded", function () {
        const deleteButtons = document.querySelectorAll(".btn-delete");

        deleteButtons.forEach(button => {
            button.addEventListener("click", function (e) {
                e.preventDefault(); // stop normal navigation

                const url = this.getAttribute("href");

                Swal.fire({
                    title: "Are you sure?",
                    text: "This action cannot be undone!",
                    icon: "warning",
                    showCancelButton: true,
                    confirmButtonColor: "#d33",
                    cancelButtonColor: "#3085d6",
                    confirmButtonText: "Yes, delete it!"
                }).then((result) => {
                    if (result.isConfirmed) {
                        Swal.fire({
                          title: "Deleted!",
                          text: "Your file has been deleted.",
                          icon: "success"
                        });

                        // Redirect to delete URL
                        window.location.href = url;
                    }
                });
            });
        });
    });
</script>

