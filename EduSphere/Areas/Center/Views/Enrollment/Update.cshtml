@model Enrollment

<div class="container">
    <div class="row mb-3 align-items-center my-3">
        <div class="col-md-6">
            <h2 class="display-6">Update Enrollment</h2>
        </div>
    </div>

    <div class="row my-3">
        <div class="col-12">
            <form method="post" enctype="multipart/form-data">
                <!-- Ensure the Edit form posts the id -->
                <input type="hidden" asp-for="EnrollmentId" />
                @*  <div class="mb-3">
                    <label for="Name" class="form-label">EnrollmentId</label>
                    <input class="form-control" placeholder="EnrollmentId" asp-for="EnrollmentId">
                    <span asp-validation-for="EnrollmentId" class="text-danger"></span>
                </div> *@

                <div class="mb-3">
                    <label for="Img" class="form-label">StudentId</label>
                    <input class="form-control" placeholder="StudentId" asp-for="StudentId">
                    <span asp-validation-for="StudentId" class="text-danger"></span>

                </div>
                <div class="mb-3">
                    <label for="Img" class="form-label">GroupId</label>
                    <input class="form-control" placeholder="GroupId" asp-for="GroupId">
                    <span asp-validation-for="GroupId" class="text-danger"></span>

                </div>
                <div class="mb-3">
                    <label for="Img" class="form-label">EnrollmentDate</label>
                    <input class="form-control" placeholder="EnrollmentDate" asp-for="EnrollmentDate">
                    <span asp-validation-for="EnrollmentDate" class="text-danger"></span>

                </div>
                <div class="mb-3">
                    <label for="Img" class="form-label">Status</label>
                    <input class="form-control" placeholder="Status" asp-for="Status">
                    <span asp-validation-for="Status" class="text-danger"></span>

                </div>

                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
    </div>
</div>

@* <script>
    function previewImage(event) {
        const input = event.target;
        const preview = document.getElementById("preview");

        if (input.files && input.files[0]) {
            const reader = new FileReader();
            reader.onload = function(e) {
                preview.src = e.target.result; // الصورة الجديدة
            }
            reader.readAsDataURL(input.files[0]);
        }
    }
</script> *@


